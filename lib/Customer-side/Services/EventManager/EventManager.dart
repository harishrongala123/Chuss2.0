import 'package:flutter/material.dart';
import 'EventManagerBody.dart';

class EventManager extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: EventManagerBody(),
    );
  }
}
